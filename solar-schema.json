{
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "name": { "type": "string" },
      "type": { "type": "string" },

      "orbit": {
        "type": "object",
        "properties": {
          "around": { "type": "string" },
          "semi_major_axis_au": { "type": "number" },
          "orbital_period_days": { "type": "number" }
        },
        "required": ["around", "semi_major_axis_au", "orbital_period_days"],
        "additionalProperties": false
      },

      "physical": {
        "type": "object",
        "properties": {
          "mean_radius_km": { "type": "number" },
          "mass_kg": { "type": "number" },
          "gravity_m_s2": { "type": "number" }
        },
        "required": ["mean_radius_km", "mass_kg", "gravity_m_s2"],
        "additionalProperties": false
      },

      "atmosphere": {
        "type": "array",
        "items": { "type": "string" }
      },

      "satellites": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "name": { "type": "string" },
            "type": { "type": "string" },
            "mean_radius_km": { "type": "number" },
            "discovery": { "type": "string" }
          },
          "required": ["name", "type", "mean_radius_km", "discovery"],
          "additionalProperties": false
        }
      },

      "discovery": {
        "type": "object",
        "properties": {
          "discovered_by": { "type": "string" },
          "year": { "type": "integer" }
        },
        "required": ["discovered_by"],
        "additionalProperties": false
      }
    },

    "required": [
      "name",
      "type",
      "orbit",
      "physical",
      "atmosphere",
      "satellites",
      "discovery"
    ],

    "additionalProperties": false
  }
}